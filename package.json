{
  "name": "typedoc-plugin-rename-defaults",
  "version": "0.6.5",
  "description": "A TypeDoc plugin that renames the `default` exports to their original name",
  "main": "dist/index.js",
  "files": [],
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:manual": "npm run build && typedoc && http-server docs -c-1 --silent -o",
    "test:manual-no-plugin": "npm run build && typedoc && http-server docs -c-1 --silent -o",
    "prepack": "rm -rf dist && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/felipecrs/typedoc-plugin-rename-defaults.git"
  },
  "keywords": [
    "typedoc",
    "plugin",
    "typedocplugin",
    "typedoc-plugin"
  ],
  "author": "Felipe Santos (https://github.com/felipecrs)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/felipecrs/typedoc-plugin-rename-defaults/issues"
  },
  "homepage": "https://github.com/felipecrs/typedoc-plugin-rename-defaults#readme",
  "peerDependencies": {
    "typedoc": "0.22.x || 0.23.x || 0.24.x"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "typedoc": "^0.24.1",
    "typedoc-plugin-rename-defaults": ".",
    "typescript": "^5.0.4"
  },
  "volta": {
    "node": "18.15.0",
    "npm": "9.6.4"
  }
}
